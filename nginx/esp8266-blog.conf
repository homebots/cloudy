server {
  listen 80;
  server_name esp8266.blog;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  server_name esp8266.blog;
  ssl on;

  location / {
    proxy_pass http://localhost:{{port}};
  }
}
